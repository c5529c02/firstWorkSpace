Primary Key: 主鍵，唯一性不允許重複
foreign key: 外鍵

資料格式:
  數值:
    smallint
    int(常用)
    bigint
    float(常用)
    real
  字串:
    char
    varchar
    nchar
    nvarchar(常用)
    
SELECT 欄位名稱
(INTO 表名)
FROM 表名
WHERE 條件敘述
GROUP BY 欄位名稱
ORDER BY 欄位名稱

